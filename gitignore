# Node modules folder (should not be pushed)
node_modules/

# Environment variables (should be kept secret)
.env

# Dependency lock files (optional, but recommended to keep out)
package-lock.json
yarn.lock

# Logs (optional but useful for avoiding large logs in repo)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE files (optional, for common IDEs like VSCode and JetBrains)
.vscode/
.idea/

# OS-generated files (optional)
.DS_Store
Thumbs.db
